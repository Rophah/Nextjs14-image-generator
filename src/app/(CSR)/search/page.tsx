import SearchPage from "./SearchPage";

export const metadata = {
    title: "Search - NextJS 14 Image Gallery"
}

const page = () => {
    return (
        <div>
            <SearchPage />
        </div>
    );
}

export default page;